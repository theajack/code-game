export enum BLOCK {
    GROUND, // 地面
    GAP,
    STEP1,
    STEP2,
    STEP3,
    STEP4,
    STEP5,
}

export enum SPIRIT {
    PERSON,
    SCORE,
    MONSTER
}

export enum ACTION {
    MOVE,
    TURN_RIGHT,
    TURN_LEFT,
    JUMP_GAP,
    JUMP_STEP
}

export enum TOOL {
    
}